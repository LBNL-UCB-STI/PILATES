# Refactor Plan Status

This file is not an active public-facing architecture guide.

The detailed content that used to live here was a speculative or intermediate
refactor roadmap tied to an older code layout, including references to modules
and responsibilities that no longer match the current repository.

## Current Status

- treat the old plan as historical design material, not current guidance
- use `docs/architecture.md` for the current architecture overview
- use `docs/workflow_primer.md` and `docs/model_integration_guide.md` for the
  current workflow and integration surface

## Where Historical Planning Notes Belong

Proposal-style and archival notes should live under `docs-internal/`.

If this refactor plan becomes active again, it should be rewritten there as a
dated decision record or implementation checklist against the current codebase,
not restored here as a public docs page.
